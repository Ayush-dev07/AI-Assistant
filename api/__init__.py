"""
FastAPI application package — Phase 5.

The app object is created in api/main.py.  This __init__.py exists to
make api/ a proper Python package so relative imports work across
the route modules.
"""