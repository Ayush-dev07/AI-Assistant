\\\After each command\\\
Context.update("last_command", command)

///In command loop (main)\\\
if "app" in entities:
            set_memory("last_app", entities["app"])
        if "value" in entities:
            set_memory(f"last_{intent}_value", entities["value"])
        if not command:
            continue
